<%= form_for(service) do |f| %>
  <% if service.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(service.errors.count, "error") %> prohibited this service from being saved:</h2>

      <ul>
      <% service.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :starting_lat %>
    <%= f.text_field :starting_lat %>
  </div>

  <div class="field">
    <%= f.label :starting_lon %>
    <%= f.text_field :starting_lon %>
  </div>

  <div class="field">
    <%= f.label :present_lat %>
    <%= f.text_field :present_lat %>
  </div>

  <div class="field">
    <%= f.label :present_lon %>
    <%= f.text_field :present_lon %>
  </div>

  <div class="field">
    <%= f.label :ending_lat %>
    <%= f.text_field :ending_lat %>
  </div>

  <div class="field">
    <%= f.label :ending_lon %>
    <%= f.text_field :ending_lon %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
